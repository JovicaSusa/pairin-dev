<div class="flex flex-col items-center w-full">
  <% if @activities.present? %>
    <% @activities.each do |activity| %>
      <div class="mb-8 px-4 py-2">
        <div>
          <p><%= activity.title %></p>
        </div>
        <div>
          <p><%= activity.content %></p>
        </div>
      </div>
    <% end %>
  <% else %>
    <p>Nothing new from community for you! But we know how to help you, you can start by creating pair request or maybe sending an offer</p>
  <% end %>
</div>
