<div class="flex flex-col items-center">
  <div class="w-full md:w-3/4 xl:w-4/6 2xl:w-1/2">
    <div class="flex w-full items-center mt-12 pb-8 border-b-4 border-black border-dashed text-center">
      <h3 class="text-5xl font-bold">Activity Feed</h3>
    </div>

    <div class="mt-12">
      <% if @activities.present? %>
        <% @activities.each do |activity| %>
          <div class="mb-8 border-2 border-black rounded-t-md shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
            <div class="border-b-2 border-black py-4 px-2 rounded-t-md bg-green">
              <p class="font-bold"><%= activity.title %></p>
            </div>
            <div class="px-2 py-4 bg-white">
              <p><%= activity.content %></p>
            </div>
          </div>
        <% end %>
      <% else %>
        <p>Nothing new from community for you! But we know how to help you, you can start by creating pair request or maybe sending an offer</p>
      <% end %>
    </div>

  </div>
</div>
