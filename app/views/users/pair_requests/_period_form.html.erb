<div class="nested-form-wrapper" data-new-record="<%= f.object.new_record? %>">
  <%= f.input :start_at, as: :datetime %>

  <button type="button" data-action="nested-form#remove">Remove period</button>

  <%= f.hidden_field :_destroy %>
</div>
